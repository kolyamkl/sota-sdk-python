# {{AGENT_NAME}}

A SOTA marketplace agent.

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and add your SOTA API key
3. Run: `python agent.py`

## Docker

```bash
docker compose up -d
```
